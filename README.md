# 🛡️ Tatar Text Detoxification  
### Детоксификация токсичных текстов на татарском языке  
**Проект для AIZAMAN 2025 — https://airi.net/ru/hackathon/aizaman-2025/**

Этот проект реализует автоматическую детоксификацию текстов на татарском языке.  
Система принимает предложения из TSV-файла, очищает их от токсичной или агрессивной лексики, смягчает тон и формирует культурную, нейтральную версию сообщения.

Метод сочетает **словарную фильтрацию токсичных слов** и **глубинную переформулировку через OpenRouter API** (GPT-4.1 или любая поддерживаемая модель).  
Все обработанные данные записываются обратно в существующую колонку TSV без изменения структуры файла.

---

## 🎯 Цель проекта

Создать инструмент для мягкой нейтрализации агрессивных сообщений, который:

- сохраняет основной смысл высказывания;
- убирает оскорбления и токсичность;
- делает коммуникацию безопаснее и дружелюбнее;
- минимально вмешивается в структуру текста.

Это позволяет не блокировать сообщения, а **переформулировать их в конструктивной форме**.

---

## 🔍 Как работает система

### 1️⃣ Чтение и обработка TSV

Система открывает входной файл `inputs.tsv`, содержащий:

- **tat_toxic** — оригинальное токсичное предложение  
- **tat_detox1** — колонка, куда будет записываться результат  

Строки обрабатываются строго по порядку, структура файла не меняется.

---

### 2️⃣ Словарная замена  

Перед использованием модели текст проходит предварительную очистку по словарю:

- ахмак → дөрес аңламаучы  
- хайван → кеше  
- җүләр → адашкан кеше  
- терлек → кеше  
- дебил → ялгышучы  
- скотина → начар кеше  

Это снижает риск, что модель ошибётся или сохранит агрессивный стиль.

---

### 3️⃣ Сессионная работа с OpenRouter API

Общение с моделью ведётся в рамках *единой чат-сессии*:

- стиль ответов стабилен;
- модель «запоминает» контекст задачи;
- качество переформулировки повышается по мере обработки строк.

Используемая модель настраивается — по умолчанию `openai/gpt-5.1`, но можно выбрать любую: Claude, DeepSeek, Qwen, Mixtral и др.

---

### 4️⃣ Генерация нейтрального текста

Модель создаёт:

- одно предложение;
- без форматирования;
- без префиксов («Вот исправленный текст:»);
- без разрывов строк;
- максимально культурную формулировку.

---

### 5️⃣ Запись результата

Смягчённый текст записывается обратно в колонку `tat_detox1` **строго в той же строке**, без создания новых строк или изменения порядка.

---

## 🧪 Пример обработки

| Исходный текст | После словаря | Финальный результат |
|----------------|---------------|---------------------|
| Син нинди ахмак кеше! | Син нинди дөрес аңламаучы кеше! | Син дөрес аңламагансың. |

---

# 🧰 Технологический стек

### 🧠 **LLM / AI**
- OpenRouter API  
- Модели: GPT-4.1, Claude 3.5, DeepSeek, Qwen, Mixtral  

### 🐍 **Backend и обработка данных**
- Python 3.x  
- `csv` — работа с TSV  
- `re` — регулярные выражения  
- OpenAI SDK (совместим с OpenRouter)

### 📄 **Формат данных**
- TSV (Tab-Separated Values)

### 🧩 **Алгоритмические методы**
- Dictionary-based detoxification  
- Generative detoxification via LLM  
- Session-based prompt consistency  


